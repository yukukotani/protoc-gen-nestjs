# protoc-gen-nestjs demo

## Usage

Install deps:

```
pnpm install
```

Generate types:

```
pnpm generate
```

Start:

```
pnpm dev
```

Now gRPC server is served on `localhost:5000`. The protobuf definition is [here](/proto/eliza.proto)
